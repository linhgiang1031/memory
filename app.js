const a=document.getElementById("app"),id=new URLSearchParams(location.search).get("m")||"demo",m=MEMORIES[id];if(!m){a.innerHTML="<p>Memory không tồn tại.</p>"}else{document.title=m.title+" · Memory";a.innerHTML=`<section class="hero"><small>A PRIVATE LITTLE GALLERY</small><h1>${m.title}</h1><div class="date">${m.date}</div><div class="message">${m.message}</div></section><section class="gallery">${m.photos.map((x,i)=>`<img src="${x}" alt="Memory ${i+1}">`).join("")}</section>`}